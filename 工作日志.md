# DataShapley 项目工作日志

> 本文档记录对数据价值评估项目 **DataShapley**（Ghorbani & Zou, ICML 2019）源码的分析工作，包含程序流程图（PAD 图）、数据流图（DFD）与 FLOPs 计算分析。

## 1 基本信息

| 项目 | 内容 |
|---|---|
| 项目名称 | DataShapley：基于 Shapley 值的数据价值评估 |
| 代码来源 | https://github.com/amiratag/DataShapley（ICML 2019 官方实现） |
| 本地路径 | `/Users/esunlee/clone_workspace/DataShapley` |
| 日志日期 | 2026-08-06 |
| 主要文件 | DShap.py / Shapley.py / shap_utils.py / Example.ipynb |
| 技术栈 | Python、NumPy、scikit-learn、TensorFlow 1.12、Matplotlib |

## 2 项目概述

项目目标：给定训练数据（点或按源分组）、学习算法与模型性能度量（准确率、F1、AUC、交叉熵等），为每个训练数据点（源）计算其在模型训练中的**边际贡献**（Shapley 值），从而公平地量化数据价值。主要应用场景：数据市场定价、训练数据筛选与去重、模型鲁棒性分析等。

系统提供三种价值估计方法：

- **TMC-Shapley**：截断蒙特卡洛采样，随机排列数据源并逐源增量训练，计算边际贡献的平均值；当增量得分接近全量得分时提前终止（截断），大幅降低计算量；

- **G-Shapley**：梯度近似，利用单轮梯度下降后每个数据源的验证分数增量近似边际贡献；

- **LOO（留一法）**：逐一剔除每个数据源重新训练，以全量模型与剔除后模型的性能差作为价值。

## 3 系统模块结构与调用关系

| 文件 | 主要类/函数 | 职责 |
|---|---|---|
| [DShap.py](DShap.py) | `DShap` | 主控类：数据管理、TMC/G/LOO 三种估值、并行结果合并、结果保存与性能绘图 |
| [Shapley.py](Shapley.py) | `ShapNN` / `CShapNN` | TensorFlow 1 神经网络封装：支持带权训练、逐源验证分数记录（G-Shapley 依赖） |
| [shap_utils.py](shap_utils.py) | `return_model` / `marginals` / `one_iteration` / `error` 等 | 模型工厂、合成数据生成、独立版 TMC 工具、评估指标与收敛判据 |
| [Example.ipynb](Example.ipynb) | — | 端到端示例：合成数据与真实数据估值、收敛图、价值-性能移除图 |
| [README.md](README.md) | — | 使用说明与论文引用信息 |

模块调用关系：

```text

Example.ipynb（调用示例）
 └─ DShap(X, y, X_test, y_test, num_test, ...)
     ├─ return_model()              shap_utils：按模型族构建模型
     ├─ init_score(metric)          计算随机/未训练基线得分
     └─ run(save_every, err, ...)    主流程
         ├─ _calculate_loo_vals()    LOO 留一值计算
         ├─ _tmc_shap() ── one_iteration() ── restart_model() / value()
         │                 └─ _tol_mean_score()
         ├─ _g_shap() ── _one_step_lr() 学习率搜索 / ShapNN.fit()
         ├─ error()                 shap_utils：收敛判据
         └─ save_results() / merge_results() ── _merge_parallel_results()
value(model, metric) ── my_accuracy_score / my_auc_score / my_xe_score / model.score
performance_plots() ── _portion_performance()

```
## 4 核心算法流程说明

### 4.1 TMC-Shapley（截断蒙特卡洛）

每次迭代随机排列所有数据源，按排列顺序逐步累加数据并重新训练模型，相邻两次得分之差即为该源的边际贡献；当累计得分与全量得分之差小于 `tolerance × mean_score` 且连续出现 6 次时提前截断。多次迭代的边际贡献取平均，误差用 `error()`（最近 100 轮相对误差）判断是否收敛。

### 4.2 G-Shapley（梯度近似）

利用 `ShapNN` 以 `batch_size=1` 做单轮 SGD 训练，模型按随机排列逐个源地“走过”数据，记录每个源加入后的验证分数，相邻分数差即近似边际贡献。学习率通过 `_one_step_lr()` 网格搜索（10^-1 ~ 10^-4.5）。

### 4.3 LOO（留一法）

全量训练得到基线得分，再逐一剔除每个数据源重新训练，`基线得分 − 剔除后得分` 即为该源的留一价值。计算量最大（需 n 次重训练），通常作为其他方法的对照基准。

### 4.4 并行与断点续算

支持多进程并行：每个进程以不同编号 `mem_XXX` 保存结果，`merge_results()` 按样本数加权合并并删除原始文件，支持断点续算（数据与中间结果以 pkl 落盘）。

## 5 程序流程图（PAD 图）

### 5.1 PAD 图例

```text

顺序结构（自上而下依次执行）：
┌────────────────┐
│ 处理1          │
├────────────────┤
│ 处理2          │
└────────────────┘

选择结构（T = 真分支 THEN，E = 假分支 ELSE）：
┌────────────────┐
│ 判断条件 C     │
│ T│ 处理P       │
│ E│ 处理Q       │
└────────────────┘

当型循环（WHILE C 为真时重复执行循环体）：
┌────────────────┐
│ WHILE C        │
│ │ 处理P        │
└────────────────┘

直到型循环（先执行循环体，直到条件 C 为真停止）：
┌────────────────┐
│ 处理P          │
│ UNTIL C        │
└────────────────┘


```
### 5.2 系统主流程 PAD 图（`DShap.run()`）

```text

┌──────────────────────────────────────────────────────────────────┐
│ 开始                                                             │
├──────────────────────────────────────────────────────────────────┤
│ 输入参数：X, y, X_test, y_test, num_test、sources、模型族/指标   │
├──────────────────────────────────────────────────────────────────┤
│ 设置随机种子；划分数据集并保存/加载 data.pkl（数据预处理）       │
├──────────────────────────────────────────────────────────────────┤
│ 创建学习模型 model = return_model(model_family, **kwargs)        │
├──────────────────────────────────────────────────────────────────┤
│ 计算随机基线得分 random_score = init_score(metric)               │
├──────────────────────────────────────────────────────────────────┤
│ IF loo_run = True                                                │
│  T│ vals_loo 尚未计算（首次运行）                                │
│   T│ _calculate_loo_vals() 计算 LOO 留一值                       │
│   │  save_results(overwrite=True) 保存结果                       │
│   E│ 沿用已加载的 vals_loo                                       │
│  E│ 跳过 LOO 计算                                                │
├──────────────────────────────────────────────────────────────────┤
│ g_run = g_run 且 model_family ∈ {logistic, NN}                   │
├──────────────────────────────────────────────────────────────────┤
│ WHILE tmc_run 或 g_run 为真                                      │
│  │ IF g_run 且 error(mem_g) < err（G 已收敛）                    │
│  │  T│ g_run = False，停止 G-Shapley                             │
│  │  E│ _g_shap(save_every) 再运行一轮                            │
│  │  │ vals_g = mean(mem_g, 0)                                    │
│  │ IF tmc_run 且 error(mem_tmc) < err（TMC 已收敛）              │
│  │  T│ tmc_run = False，停止 TMC-Shapley                         │
│  │  E│ _tmc_shap(save_every, tolerance) 再运行一轮               │
│  │  │ vals_tmc = mean(mem_tmc, 0)                                │
│  │ IF directory 非空 → save_results() 保存中间结果               │
├──────────────────────────────────────────────────────────────────┤
│ 输出：vals_loo / vals_tmc / vals_g（各数据点（源）的价值）       │
├──────────────────────────────────────────────────────────────────┤
│ 结束                                                             │
└──────────────────────────────────────────────────────────────────┘

```
### 5.3 TMC-Shapley 单次迭代 PAD 图（`one_iteration()`）

```text

┌──────────────────────────────────────────────────────────────────┐
│ 输入：tolerance（截断容差）、sources（数据源分组）               │
├──────────────────────────────────────────────────────────────────┤
│ idxs = 对全部数据源随机排列（permutation）                       │
├──────────────────────────────────────────────────────────────────┤
│ 初始化：marginal_contribs 全 0 向量；X_batch / y_batch 为空      │
│ new_score = random_score（未训练模型基线）                       │
├──────────────────────────────────────────────────────────────────┤
│ FOR 按排列顺序逐一加入数据源（n ∈ idxs）                         │
│  │ old_score = new_score（记录加入前得分）                       │
│  │ 将该源数据拼接到 X_batch / y_batch（增量累积）                │
│  │ IF 满足训练条件（包含全部类别 或 回归问题）                   │
│  │  T│ restart_model() 重置为未训练模型                          │
│  │  │ model.fit(X_batch, y_batch) 重新训练                       │
│  │  │ new_score = value(model) 在测试集上评估                    │
│  │ 边际贡献 = (new_score − old_score) / 源内点数                 │
│  │ marginal_contribs[该源] = 边际贡献                            │
│  │ IF |new_score − mean_score| ≤ tolerance × mean_score          │
│  │  T│ truncation_counter += 1（连续截断计数）                   │
│  │  │ IF truncation_counter > 5 → break 截断提前终止             │
│  │  E│ truncation_counter = 0（重置计数）                        │
├──────────────────────────────────────────────────────────────────┤
│ 返回 marginal_contribs、idxs（供 _tmc_shap 累积）                │
└──────────────────────────────────────────────────────────────────┘

```
### 5.4 G-Shapley PAD 图（`_g_shap()`）

```text

┌──────────────────────────────────────────────────────────────────┐
│ 输入：iterations、learning_rate（可为 None）、sources            │
├──────────────────────────────────────────────────────────────────┤
│ IF learning_rate = None                                          │
│  T│ 已缓存 g_shap_lr 学习率？                                    │
│   T│ 直接使用缓存学习率                                          │
│   E│ _one_step_lr() 网格搜索最优学习率（10^-1 ~ 10^-4.5）        │
│  E│ 使用传入的学习率                                             │
├──────────────────────────────────────────────────────────────────┤
│ 创建 ShapNN 模型（batch_size=1、SGD、max_epochs=1）              │
├──────────────────────────────────────────────────────────────────┤
│ FOR iteration = 1 .. iterations                                  │
│  │ model.fit(空数据) 重置网络权重                                │
│  │ model.fit(X, y, X_test, y_test, sources, metric, 1 轮)        │
│  │ 按采样顺序提取逐源验证分数 val[0..m]                          │
│  │ 逐点边际贡献：marginal[i] = val[i] − val[i−1]                 │
│  │ 按 sources 累加得到 individual_contribs（逐点价值）           │
│  │ mem_g / idxs_g 追加本次迭代结果                               │
├──────────────────────────────────────────────────────────────────┤
│ 结束（得到 G-Shapley 近似价值）                                  │
└──────────────────────────────────────────────────────────────────┘

```
### 5.5 LOO 留一法 PAD 图（`_calculate_loo_vals()`）

```text

┌──────────────────────────────────────────────────────────────────┐
│ 输入：sources、metric（默认 accuracy）                           │
├──────────────────────────────────────────────────────────────────┤
│ restart_model() 重置模型                                         │
├──────────────────────────────────────────────────────────────────┤
│ model.fit(X, y) 使用全部训练数据训练                             │
├──────────────────────────────────────────────────────────────────┤
│ baseline_value = value(model) 评估全量模型得分                   │
├──────────────────────────────────────────────────────────────────┤
│ FOR 每个数据源 i ∈ sources.keys()                                │
│  │ 从训练集中删除该源：X_batch / y_batch = delete(X, y, i)       │
│  │ model.fit(X_batch, y_batch) 用剩余数据重新训练                │
│  │ removed_value = value(model) 评估剔除后模型得分               │
│  │ vals_loo[该源] = (baseline_value − removed_value) / 源内点数  │
├──────────────────────────────────────────────────────────────────┤
│ 返回 vals_loo（各数据源留一价值）                                │
└──────────────────────────────────────────────────────────────────┘

```
## 6 数据流图（DFD）

### 6.1 顶层图（上下文图）

```text

训练数据X,y / 测试数据X_test,y_test / num_test / sources / 参数配置
         │──────────────────────────┐                             
         │                          ▼                             
┌──────────────┐                    ┌──────────────────┐          
│ 用户/调用者  │                         │ 0 数据Shapley值  │     
│ （外部实体） │                          │ 计算系统         │    
└──────────────┘                    └──────────────────┘          
         ▲                          │                             
         └──────────────────────────┘                             
         数据点价值（Shapley 值）/ 结果文件 *.pkl / 图表 *.png    

```
### 6.2 0 层图（系统内部分解）

> 说明：外部实体为调用者（用户/示例程序）；D1~D5 为数据存储（pkl 文件/目录）。

```text

                            ┌────────────────────┐                              
                            │ 用户/调用者        │                              
                            │ （外部实体）       │                              
                            └─────────│ (1) 训练/测试数据、参数、sources        
                                      ▼                                         
                          ┌────────────────────────┐                            
                          │ 1 数据预处理与初始化   │         (2) 划分后数据─────────────┐
                          └───────────┬────────────┘   ───────▶ │ D1 数据集    │
                                      │ (3) 数据与测试集        │ data.pkl     │
                                      ▼                         └──────────────┘
                          ┌────────────────────────┐                            
                          │ 2 基线得分计算         │                            
                          │ （init_score）         │                            
                          └───────────┬────────────┘                            
                                      │ (4) 随机基线得分                        
                                      ▼                                         
             ┌────────────────────────┼──────────────────────────┐              
             ▼                        ▼                          ▼              
     ┌────────────────┐       ┌────────────────┐         ┌────────────────┐     
     │ 3 LOO 留一计算 │           │ 4 TMC-Shapley  │         │ 5 G-Shapley 计算│
     │ （留一法）     │            │    计算        │           │                │
     └───────┬────────┘       └───────┬────────┘         └───────┬────────┘     
             │ (7) LOO 值             │ (6) TMC 贡献             │ (8) G 贡献   
             ▼                        ▼                          ▼              
     ┌────────────────┐       ┌────────────────┐         ┌────────────────┐     
     │ D4 loo.pkl     │       │ D2 mem_tmc     │         │ D3 mem_g       │     
     │                │       │   *.pkl        │         │   *.pkl        │     
     └───────┬────────┘       └───────┬────────┘         └───────┬────────┘     
             │                        │                          │              
             └────────────────────────┴──────────────────────────┘              
                                      │ (9) 各类价值结果汇总                    
                                      ▼                                         
                          ┌────────────────────────┐                            
                          │ 6 结果合并、保存       │                            
                          │ 与绘图                 │                            
                          └───────────┬────────────┘                            
                                      │ (10) 数据点价值、结果文件、图表         
                                      ▼                                         
                            ┌────────────────────┐                              
                            │ 用户/调用者        │                              
                            │ （外部实体）       │                              
                            └────────────────────┘                              
注：数据流 (5)（D1 → 3/4/5，断点续算读取历史数据）为简化未画出，详见 6.4 数据字典。

```
### 6.3 1 层图（过程 4：TMC-Shapley 计算细化）

```text

(4-1) X, y、X_test, y_test、tolerance、sources（来自过程 1/2 与 D1）          
                             │                                                
                             ▼                                                
                ┌──────────────────────────┐                                  
                │ 4.1 计算平均得分与容差   │                                  
                │ （_tol_mean_score）      │                                  
                └────────────┬─────────────┘                                  
                             │ (4-2) mean_score、tolerance                    
                             ▼                                                
                ┌──────────────────────────┐                                  
                │ 4.2 单次 TMC 迭代        │                                  
                │ （one_iteration）        │                    ┌──────────────┐
                │ ·随机排列数据源          │       ─(4-3) 边际贡献─▶ │ D2 边际贡献  │
                │ ·逐源增量训练并评估      │                           │ 矩阵文件     │
                │ ·截断容差判断            │                        │ mem_tmc_*.pkl│
                └──────────────────────────┘                  └──────────────┘
                             │ (4-4) marginal_contribs、idxs                  
                             ▼                                                
                ┌──────────────────────────┐                                  
                │ 4.3 边际贡献累积         │                                  
                │ （mem_tmc 拼接）         │                                  
                └────────────┬─────────────┘                                  
                             │ (4-5) mem_tmc                                  
                             ▼                                                
                ┌──────────────────────────┐                                  
                │ 4.4 收敛判断             │    ─── 未收敛 ───▶ 返回 4.2      
                │ error(mem_tmc) < err     │                                  
                └────────────┬─────────────┘                                  
                             │ (4-6) 已收敛                                   
                             ▼                                                
                ┌──────────────────────────┐                                  
                │ 4.5 输出均值价值         │                                  
                │ vals_tmc = mean(mem_tmc) │                                  
                └──────────────────────────┘                                  

```
### 6.4 数据字典（部分）

| 编号 | 名称 | 类型 | 源 → 终点 | 说明 |
|---|---|---|---|---|
| (1) | 输入数据与参数 | 数据流 | 用户 → 1 | X、y、X_test、y_test、num_test、sources、模型族、指标、目录 |
| (2) | 划分后数据 | 数据流 | 1 → D1 | 划分为 X/X_test/X_heldout 并落盘 |
| (3) | 数据与测试集 | 数据流 | 1 → 2 → 3/4/5 | 内存传递，供训练与评估 |
| (4) | 随机基线得分 | 数据流 | 2 → 3/4 | random_score，未训练模型/众数基线 |
| (5) | 读取数据 | 数据流 | D1 → 3/4/5 | 断点续算时加载已保存数据（图中未画出） |
| (6) | TMC 边际贡献 | 数据流 | 4 → D2 | mem_tmc（迭代数 × 点数）、idxs_tmc |
| (7) | LOO 值 | 数据流 | 3 → D4 | vals_loo 数组 |
| (8) | G 边际贡献 | 数据流 | 5 → D3 | mem_g、idxs_g |
| (9) | 价值结果汇总 | 数据流 | D2/D3/D4 → 6 | 加权合并并行结果 |
| (10) | 数据点价值与图表 | 数据流 | 6 → 用户 | Shapley 值、pkl 结果文件、PNG 图 |
| D1 | 数据集文件 | 数据存储 | — | data.pkl：X、y、X_test、X_heldout、sources |
| D2 | TMC 结果 | 数据存储 | — | mem_tmc_XXXX.pkl：边际贡献矩阵与采样顺序 |
| D3 | G 结果 | 数据存储 | — | mem_g_XXXX.pkl：同上 |
| D4 | LOO 结果 | 数据存储 | — | loo.pkl：vals_loo |
| D5 | 图表目录 | 数据存储 | — | plots/*.png：性能移除图、收敛图 |

## 7 FLOPs 计算分析

### 7.1 计算口径

与仓库内 `C3D-I3D-LSTM网络结构与参数量-FLOPs.xlsx`（thop / pytorch-OpCounter 风格）保持一致：

- 全连接层：参数量 = `out×in + out`；FLOPs = `out×(2×in − 1)`（单样本）；
- 卷积层：参数量 = `out×(in×k²) + out`；FLOPs = `out×W×H×(2×in×k² + 1)`（单样本）；
- 激活函数（每元素）：relu = 1、sigmoid = 3、tanh = 7、BN = 4、softmax = `3×类别数 − 1`；
- 汇总表 FLOPs = **批处理大小 × 单样本前向 FLOPs**。

### 7.2 各模型单次前向参数量与 FLOPs（输入 d=100，二分类）

| 模型 | 批处理大小 | 参数量 | FLOPs（单批前向） |
|---|---|---|---|
| logistic（LogisticRegression，100→2 类） | 256 | 2.02×10² | 1.03×10⁵ |
| NN（MLPClassifier，100→20→2，默认隐藏层 (20,)） | 256 | 2.06×10³ | 1.05×10⁶ |
| ShapNN（G-Shapley，100→2，代码中 batch_size=1） | 1 | 2.02×10² | 4.03×10² |
| ShapNN（G-Shapley，100→20→2） | 1 | 2.06×10³ | 4.08×10³ |
| conv（CShapNN，5×5 卷积 stride=5 → 20 → 2） | 256 | 8.09×10³ | 9.44×10⁶ |

> 注：树 / 随机森林 / KNN / SVM / 朴素贝叶斯等无学习权重（参数量 = 0），FLOPs 为比较/距离运算，不适用卷积/FC 口径，未列入。

### 7.3 三种算法总开销估算（n=1000，d=100，logistic）

估算假设：liblinear 平均迭代 n_iter=20；TMC 迭代 100 次、截断后平均使用前 60% 数据；G 迭代 100 轮；测试集 \|X_test\|=1000；单次 fit（规模 k）≈ `n_iter×2×k×d`。

| 算法 | 计算构成 | FLOPs | 占比 |
|---|---|---|---|
| LOO | (n+1) 次全量训练（≈4×10⁶/次）+(n+1) 次测试评估（≈4×10⁵/次） | ≈ 4.4×10⁹ | 3% |
| TMC | 100 次迭代 ×（Σ_{k=1..600} 4000k + 600 次测试评估×4×10⁵ ≈ 9.6×10⁸/次） | ≈ 9.6×10¹⁰ | 68% |
| G-Shapley | 学习率搜索 160 次小训练（≈2×10⁸）+ 100 轮 ShapNN fit（每轮 n 步：1 样本训练 + 全测试集评估，≈4×10⁸/轮） | ≈ 4.1×10¹⁰ | 29% |
| **总计** | | **≈ 1.41×10¹¹** | 100% |

### 7.4 结论与注意事项

1. 单次前向远小于 C3D/I3D 等视频模型（小约 5 个数量级），项目总成本集中在**训练次数**（每次 fit 从头训练、随规模 O(n²) 累加），而非单次前向；
2. 本实现中 G-Shapley 与 TMC 同量级：`batch_size=1` 导致 n 步训练，且每步都对整个测试集评估一次，抵消了论文中 G 的加速优势；
3. 以上为估算值，精确值需插桩统计实际训练次数（TMC 截断、G 收敛均会提前终止），见第 10 节计划。

## 8 工作进度记录

| 日期 | 工作内容 | 结果/产出 |
|---|---|---|
| 2026-08-06 上午 | 克隆仓库，通读 README 与目录结构 | 明确项目定位与模块划分 |
| 2026-08-06 上午 | 精读 DShap.py：初始化、run 主流程、TMC/G/LOO 三种算法 | 梳理出系统主流程与三种估值流程（见第 5 节） |
| 2026-08-06 下午 | 阅读 shap_utils.py、Shapley.py | 掌握模型工厂、评估指标、收敛判据与 ShapNN 封装细节 |
| 2026-08-06 下午 | 绘制 PAD 图（主流程、TMC、G-Shapley、LOO 共 4 张） | 见第 5.2~5.5 节 |
| 2026-08-06 下午 | 绘制数据流图（顶层/0 层/1 层）并整理数据字典 | 见第 6.1~6.4 节 |
| 2026-08-06 下午 | 汇总问题清单，整理本文档 | 见第 9~10 节 |
| 2026-08-06 下午 | 计算模型前向 FLOPs 与 LOO/TMC/G 三种算法总开销，整理 FLOPs 分析 | 见第 7 节 |
| 2026-08-07 | 生成 DataShapley网络结构与参数量-FLOPs.xlsx（逐层公式联动，41 项公式校验全部通过） | 仓库根目录 |
| 2026-08-14 | 确认正式G-Shapley口径并优化运行方案 | 3组×50次排列；固定5,000点测试集；逐batch精确GPU AUC；评估约为训练核心FLOPs的13倍 |
| 2026-08-14 | 核验PLOVAD官方decoder源码 | `src/model.py:109-113`两层Conv1d均为`kernel_size=1, padding=0`，当前两层逐片段MLP建模正确 |
| 2026-08-14 | 确定正式优化器与学习率校准方法 | 改用无动量SGD；8个候选学习率×3个固定seed进行one-pass校准，以`mean(AUC)-std(AUC)`选优 |
| 2026-08-14 | 开始实现`gshap_pytorch/` | 已完成配置、数据拆分、PLOVAD decoder、精确GPU AUC、SGD学习率校准、核心训练器、iteration级断点、三组汇总及基础测试；待远程PyTorch环境执行测试和真实基准 |
| 2026-08-14 | 整理远程运行前置条件 | 新增`远程运行环境确认清单.md`，等待GPU、CUDA、路径、调度器等信息 |

## 9 发现的问题与注意事项

1. **[shap_utils.py:298](shap_utils.py#L298) 变量名笔误**：`my_f1_score` 中 `clf.predict(x)` 的 `x` 未定义（应为 `X`），调用该函数会触发 `NameError`。

2. **[DShap.py:60](DShap.py#L60) 字符串比较使用 `is`**：`self.model_family is 'logistic'` 依赖 CPython 字符串驻留，建议改为 `==`。

3. **依赖 TensorFlow 1.12**：使用 `tf.gfile`、`tf.random.set_random_seed` 等旧接口，现代 Python/TF 2.x 环境无法直接运行，需迁移（TF2 / PyTorch / 纯 sklearn）。

4. **`_tol_mean_score` 的 bagging 外层循环**：`for _ in range(1)` 实际只执行一次，注释与实现不一致（100 次仅是对测试集的自助采样次数）。

5. **计算开销**：`one_iteration` 每加入一个源即全量重训练（O(n²)），TMC 的截断机制用于缓解；大规模数据需增量学习或更粗粒度采样。

6. **回归问题未实现**：`is_regression` 分支仅打印警告，评估指标不支持回归。

## 10 下一步计划

- [ ] 修复第 9 节问题 1、2（`x` → `X`、`is` → `==`）；

- [ ] 评估 TensorFlow 1 → 2 迁移成本，或改用 PyTorch/sklearn 实现 `ShapNN` 等价功能；

- [ ] 在 Example.ipynb 的合成数据上跑通端到端验证，并对比 TMC/G/LOO 三者结果；

- [ ] 插桩统计 LOO/TMC/G 各阶段实际训练次数与截断比例，校准第 7 节 FLOPs 估算；

- [ ] 补充单元测试与 CI 配置。

## 11 G-Shapley 正式实验决策（2026-08-14）

### 11.1 最短时间且保持既定定义正确的配置

- 正式实验为3个独立组，每组50次 G-Shapley permutation，共150次；每次重新初始化模型和optimizer并执行一次one-pass epoch。
- batch size固定为128，batch的AUC增量除以实际batch大小后均摊给batch内样本。
- 从704,459点中固定分层抽取5,000点作正式测试集，并另抽5,000点作学习率验证集，约剩694,459点参与训练和估值；全长输出在两个保留集位置填`NaN`。
- 正式优化器改为`SGD(momentum=0, weight_decay=0)`，与G-Shapley参考实现一致；PLOVAD的Adam(`lr=1e-3`)只作原配置记录。
- 学习率候选为`1e-1, 10^-1.5, 1e-2, 10^-2.5, 1e-3, 10^-3.5, 1e-4, 10^-4.5`。每个候选在固定100K分层训练子集上用3个seed各做一次one-pass，只在结束时计算学习率验证集AUC，以`mean(AUC)-std(AUC)`选优；随后在完整训练子集做一次one-pass稳定性确认并冻结学习率。
- 测试集常驻GPU；显存允许时训练特征也常驻GPU。每个batch后直接在GPU计算精确AUC，禁止逐步转CPU调用`roc_auc_score`；每次排列结束后才落盘。
- 只做一次真实`1组×1排列`基准，分别测训练、测试前向、GPU AUC、I/O和wall time，验证无误后直接外推并运行正式任务。

### 11.2 成本量化

按`n_train≈694,459`、`n_test=5,000`、batch size 128估计：每次排列约5,426个batch，训练核心计算约0.274 TFLOPs，逐batch测试前向约3.56 TFLOPs；评估约为训练的13倍，占二者核心FLOPs约92.9%。相比10%（约70K）测试集的约184倍评估/训练比，固定5,000点可将评估部分缩短约14倍。

### 11.3 PLOVAD decoder核验结论

已核验PLOVAD官方仓库`master/src/model.py`第109～113行：

```python
self.classifier = nn.Sequential(
    nn.Conv1d(512, cfg.cls_hidden, kernel_size=1, padding=0),
    nn.GELU(),
    nn.Conv1d(cfg.cls_hidden, 1, kernel_size=1, padding=0)
)
```

结论：两层`kernel_size`均为1，当前`512→128→1` decoder结构正确；当`cfg.cls_hidden=128`且两层带bias时，参数量为65,793。前向核心权重乘加FLOPs为`2×(512×128)+2×(128×1)=131,328`，不含bias和GELU。


---
*日志生成于 2026-08-06*
