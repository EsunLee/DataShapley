# G-Shapley 远程运行环境确认清单

> 用途：在正式运行前确认设备、软件、路径和调度条件。请填写“回答”列，或直接执行文末采集命令并把输出发回。

## 1. 必须确认

| 项目 | 回答 |
|---|---|
| 远程操作系统及版本 | 待填写 |
| GPU型号 | 待填写 |
| GPU数量 | 待填写 |
| 每张GPU显存 | 待填写 |
| 是否允许3张GPU各跑1组 | 待填写 |
| NVIDIA driver版本 | 待填写 |
| CUDA runtime版本 | 待填写 |
| Python版本 | 待填写 |
| PyTorch版本 | 待填写 |
| `torch.cuda.is_available()` | 待填写 |
| NumPy版本 | 待填写 |
| 是否可创建独立conda/venv环境 | 待填写 |
| 是否允许安装缺失Python包 | 待填写 |
| 远程数据目录绝对路径 | 待填写 |
| 远程代码目录绝对路径 | 待填写 |
| 远程结果目录绝对路径 | 待填写 |
| 结果盘可用空间 | 待填写 |
| 单作业最长运行时间 | 待填写 |
| 使用Slurm/PBS/其他调度器或直接shell | 待填写 |

## 2. 运行策略确认

1. 正式范围是否仍为本仓库3组：`5922, 13764, 14933`？回答：待填写。
2. 如果有3张及以上GPU，是否同意每组独占一张GPU并行？回答：待填写。
3. 如果只有1张GPU，是否同意3组串行，以保证计时可比？回答：待填写。
4. 是否需要生成Slurm/PBS提交脚本？如需要，请提供分区、账户、QoS、最长时间和GPU申请格式。回答：待填写。
5. 是否要求运行日志同时写文件（推荐）？回答：待填写。
6. 正式结果是否需要保留每次排列的完整`marginal`和`permutation`？默认不保留，只保存seed、哈希、在线均值和checkpoint，可显著减少I/O。回答：待填写。

## 3. 显存与数据加载决策

当前数据约为：

- 全部特征：约1.44 GB；
- 正式训练特征：约1.42 GB；
- 测试特征：约10 MB；
- 模型与临时张量较小。

若单卡可用显存充足，优先把训练集与测试集常驻GPU；否则训练集使用pinned memory分批传输，测试集仍常驻GPU。请提供正式运行前空闲状态下的`nvidia-smi`输出。

## 4. 一键环境采集命令

在远程代码目录执行：

```bash
python3 - <<'PY'
import os
import platform
import shutil
import sys

print("platform:", platform.platform())
print("python:", sys.version.replace("\n", " "))
print("cwd:", os.getcwd())
print("disk_free_GB:", round(shutil.disk_usage(os.getcwd()).free / 1024**3, 2))

for name in ("numpy", "torch", "pytest", "thop"):
    try:
        module = __import__(name)
        print(name + ":", getattr(module, "__version__", "installed"))
    except Exception as exc:
        print(name + ": MISSING", repr(exc))

try:
    import torch
    print("cuda_available:", torch.cuda.is_available())
    print("torch_cuda:", torch.version.cuda)
    print("cudnn:", torch.backends.cudnn.version())
    print("gpu_count:", torch.cuda.device_count())
    for index in range(torch.cuda.device_count()):
        prop = torch.cuda.get_device_properties(index)
        print("gpu", index, prop.name, "memory_GB", round(prop.total_memory / 1024**3, 2))
except Exception:
    pass
PY

nvidia-smi
```

## 5. 数据文件检查命令

把`DATA_DIR`改成远程实际路径：

```bash
DATA_DIR=/absolute/path/to/packed_data
ls -lh "$DATA_DIR"
python3 - "$DATA_DIR" <<'PY'
import pathlib
import sys
import numpy as np

root = pathlib.Path(sys.argv[1])
x = np.load(root / "ucf_train_feat_704459x512.npy", mmap_mode="r")
y = np.load(root / "ucf_train_label_704459x1.npy", mmap_mode="r")
print("X:", x.shape, x.dtype)
print("y:", y.shape, y.dtype, "classes:", np.unique(y, return_counts=True))
PY
```

预期结果：`X=(704459,512), float32`，`y=(704459,1), float32`，类别数量为592,665和111,794。

## 6. 正式开跑前的门槛

只有以下条件全部通过才开始150次正式排列：

- 数据shape、dtype和类别数量正确；
- PLOVAD decoder参数量为65,793；
- SGD学习率校准完成并写出`lr_search.csv/json`；
- 合成冒烟测试通过；
- 真实数据1组×1次排列通过效率恒等式；
- 已得到单次排列wall time，并确认总运行时间可接受；
- checkpoint中断恢复测试通过；
- 结果目录剩余空间满足要求。
