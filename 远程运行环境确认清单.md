# G-Shapley 远程运行环境确认清单

> **本文件是远程机器的标准说明文档**：包含远程机器的硬件、软件环境、路径、运行策略与开跑门槛状态。
> **约定：每次在远程机器上工作前，先读取本文件**（远程仓库中已配 CLAUDE.md 指向本文件）。
> 状态：**2026-08-14 已采集并确认**（环境采集命令见第 4 节，输出见 `env_check.txt`）。
> 维护：环境或策略变化时更新本文件并提交。

## 1. 必须确认

| 项目 | 回答 |
|---|---|
| 远程操作系统及版本 | Linux（桌面环境，Xorg + GNOME；具体发行版待补） |
| GPU型号 | 4× NVIDIA GeForce RTX 3090 |
| GPU数量 | 4 |
| 每张GPU显存 | 24,268 MiB ≈ 24 GB |
| 是否允许3张GPU各跑1组 | ✅ 已确认：GPU 0/1/2 空闲可并行；GPU 3 有桌面占用（约 0.4GB），但 24GB 下余量充足，4 卡均可使用 |
| NVIDIA driver版本 | 495.29.05（最高支持 CUDA 11.5） |
| CUDA runtime版本 | 驱动 11.5；torch 内置 cu118（实测 GPU kernel 可跑，兼容性优于官方表） |
| Python版本 | 3.10.20（`/data/conda_envs/insight_env`，已实测可用） |
| PyTorch版本 | **2.5.1+cu118**（insight_env；与计划目标 2.5.1+cu121 等价，本机驱动下可用） |
| `torch.cuda.is_available()` | True（且 GPU kernel 实测 OK：matmul 通过、`gpu_count=4`） |
| NumPy版本 | 2.2.6（兼容 torch 2.5.1） |
| 是否可创建独立conda/venv环境 | ✅ 可以（anaconda 位于 `/home/amax/anaconda3`；当前方案用现有 insight_env，如需独立可 `conda create -p /data/conda_envs/gshap_env --clone /data/conda_envs/insight_env`） |
| 是否允许安装缺失Python包 | pytest / thop 缺失，已建议补装（`pip install pytest thop -i https://pypi.tuna.tsinghua.edu.cn/simple`）；insight_env 为共享环境，装包前请确认 |
| 远程数据目录绝对路径 | `/data/ldy/DataShapley/packed_data`（**数据尚未到位**，待传输 630MB 的 packed_data.7z 或由王世成放置） |
| 远程代码目录绝对路径 | `/data/ldy/DataShapley` |
| 远程结果目录绝对路径 | `/data/ldy/DataShapley/gshap_pytorch/results` |
| 结果盘可用空间 | /data 剩余约 600 GB（充足）；/home 剩余约 60 GB |
| 单作业最长运行时间 | 无调度器限制，直接 shell 运行（待确认是否需要超时控制） |
| 使用Slurm/PBS/其他调度器或直接shell | 直接 shell（未见调度器） |

## 2. 运行策略确认

1. 正式范围是否仍为本仓库3组：`5922, 13764, 14933`？**回答：✅ 是**（seeds_used.json）。
2. 如果有3张及以上GPU，是否同意每组独占一张GPU并行？**回答：✅ 是**（3 组分别绑 GPU 0/1/2；GPU 3 备用于重试/校准）。
3. 如果只有1张GPU，是否同意3组串行，以保证计时可比？**回答：不适用**（有 4 卡）。
4. 是否需要生成Slurm/PBS提交脚本？**回答：不需要**（直接 shell）。
5. 是否要求运行日志同时写文件（推荐）？**回答：✅ 是**（建议 tee 输出到 `results/logs/`）。
6. 正式结果是否需要保留每次排列的完整`marginal`和`permutation`？**回答：默认不保留**，只保存 seed、哈希、在线均值和 checkpoint，可显著减少 I/O。

## 3. 显存与数据加载决策

数据规模：

- 全部特征：约 1.44 GB；
- 正式训练特征：约 1.42 GB；
- 测试特征：约 10 MB；
- 模型与临时张量较小（65K 参数）。

**结论**：单卡 24 GB 显存完全足够，**训练集与测试集均可常驻 GPU**，无需 pinned memory 分批传输。逐 batch 评估在 GPU 上计算 AUC（计划 §3.2），避免每步 CPU 往返。

## 4. 一键环境采集命令

已在远程执行（2026-08-14，输出存 `/data/ldy/DataShapley/env_check.txt`）。

```bash
python3 - <<'PY'
import os
import platform
import shutil
import sys

print("platform:", platform.platform())
print("python:", sys.version.replace("\n", " "))
print("cwd:", os.getcwd())
print("disk_free_GB:", round(shutil.disk_usage(os.getcwd()).free / 1024**3, 2))

for name in ("numpy", "torch", "pytest", "thop"):
    try:
        module = __import__(name)
        print(name + ":", getattr(module, "__version__", "installed"))
    except Exception as exc:
        print(name + ": MISSING", repr(exc))

try:
    import torch
    print("cuda_available:", torch.cuda.is_available())
    print("torch_cuda:", torch.version.cuda)
    print("cudnn:", torch.backends.cudnn.version())
    print("gpu_count:", torch.cuda.device_count())
    for index in range(torch.cuda.device_count()):
        prop = torch.cuda.get_device_properties(index)
        print("gpu", index, prop.name, "memory_GB", round(prop.total_memory / 1024**3, 2))
except Exception:
    pass
PY

nvidia-smi
```

> 补充：conda env list 显示 12+ 个环境；重点核验的 `/data/conda_envs/insight_env`（torch 2.5.1+cu118，**实测可用**）、`stage2_env`（torch 2.5.1+cu118，**报 driver too old，不可用**）、base（torch 1.13.1+cu117，实测可用，作后备）。

## 5. 数据文件检查命令

数据到位后执行，`DATA_DIR` 为远程实际路径：

```bash
DATA_DIR=/data/ldy/DataShapley/packed_data
ls -lh "$DATA_DIR"
python3 - "$DATA_DIR" <<'PY'
import pathlib
import sys
import numpy as np

root = pathlib.Path(sys.argv[1])
x = np.load(root / "ucf_train_feat_704459x512.npy", mmap_mode="r")
y = np.load(root / "ucf_train_label_704459x1.npy", mmap_mode="r")
print("X:", x.shape, x.dtype)
print("y:", y.shape, y.dtype, "classes:", np.unique(y, return_counts=True))
PY
```

预期结果：`X=(704459,512), float32`，`y=(704459,1), float32`，类别数量为592,665和111,794。

## 6. 正式开跑前的门槛

只有以下条件全部通过才开始150次正式排列：

| 门槛 | 状态 |
|---|---|
| 数据 shape、dtype 和类别数量正确 | ⏳ 待数据到位后远程核验（本地已核验 ✓） |
| PLOVAD decoder 参数量为 65,793 | ⏳ 待跑 `test_model.py` |
| SGD 学习率校准完成并写出 `lr_search.csv/json` | ⏳ 未做 |
| 合成冒烟测试通过 | ⏳ 未做 |
| 真实数据 1组×1次排列通过效率恒等式 | ⏳ 未做 |
| 已得到单次排列 wall time，并确认总运行时间可接受 | ⏳ 未做 |
| checkpoint 中断恢复测试通过 | ⏳ 未做 |
| 结果目录剩余空间满足要求 | ✅ /data 余 600G，充足 |
| 环境：torch 2.5.1 + GPU kernel 实测可用 | ✅ 已确认（insight_env） |
| pytest / thop 已安装 | ⏳ 待补装 |
